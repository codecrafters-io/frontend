import { gutter } from 'codecrafters-frontend/utils/codemirror-gutter-rs';
import { module, test } from 'qunit';

module('Unit | Utility | codemirror-gutter-rs', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    const result = gutter({});
    assert.ok(result);
  });
});
