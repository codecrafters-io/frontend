<ModalBody @allowManualClose={{true}} @onClose={{(noop)}} class="w-full">
  <div class="mb-6 font-semibold text-2xl text-gray-600 mr-8">
    Invoice Details
  </div>

  <div class="prose prose-sm mb-4">
    <p>
      If you need to include your employer's details on your invoice, select the option below.
    </p>
  </div>

  <div
    class="flex items-center justify-between gap-8 border {{if this.temp 'border-teal-400 bg-teal-50' 'border-gray-200'}} rounded p-4 mb-6"
    {{on "click" (fn (mut this.temp) (not this.temp))}}
    role="button"
  >
    <span class="flex flex-grow flex-col">
      <span class="text-sm font-semibold leading-6 {{if this.temp 'text-teal-600' 'text-gray-900'}}" id="availability-label">I need extra details on
        my invoice</span>
      <span class="text-xs text-gray-400" id="availability-description">Choose this option if you need extra details like tax ID, business address
        etc. on your invoice.</span>
    </span>

    <Toggle @isOn={{this.temp}} />
  </div>

  {{#if this.temp}}
    <Alert @type="success" @icon="check-circle" class="mb-4">
      <p class="mb-0">
        During checkout you'll be able to enter extra details like tax ID, business address etc. that'll be included on your invoice.
      </p>
    </Alert>
  {{/if}}

  <div class="prose prose-sm mb-6">
    <p>
      You can always email us at
      <a href="mailto:hello@codecrafters.io">hello@codecrafters.io</a>
      if you need to change your invoice details later.
    </p>
  </div>

  <div class="flex justify-between items-center">
    <span class="text-gray-400 text-sm" role="button">
      ← Back
    </span>
    <PrimaryButton @size="large">
      Proceed to Checkout →
    </PrimaryButton>
  </div>
</ModalBody>