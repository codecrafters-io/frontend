<div class="grid grid-cols-3 gap-4">
  {{#each this.features as |feature|}}
    <CreateTeamPage::FeaturesListItem @title={{feature.title}} @description={{feature.description}} @imageURL={{feature.imageURL}} />
  {{/each}}
</div>