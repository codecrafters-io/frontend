<DangerButton
  class="relative border-0 overflow-hidden"
  {{on "mouseenter" this.handleMouseEnter}}
  {{on "mouseleave" this.handleMouseLeave}}
  {{! template-lint-disable no-pointer-down-event-binding }}
  {{on "mousedown" this.startProgress}}
  {{on "mouseup" this.stopProgress}}
  data-test-delete-repository-button
  ...attributes
>
  {{yield}}

  {{#if this.isHovered}}
    <div class="absolute inset-0 bg-red-100 opacity-50" style={{this.progressBarWidthStyle}}></div>
  {{/if}}
</DangerButton>