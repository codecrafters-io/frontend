<div class="flex justify-between items-center mb-10">
  <h1 class="text-2xl text-gray-600">
    {{@course.name}}
  </h1>

  {{#if @repositories.firstObject}}
    <div>
      <CoursePageHeader::RepositoriesDropdown @repositories={{@repositories}} />
    </div>
  {{/if}}
</div>