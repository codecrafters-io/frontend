{{#if this.text}}
  <span class="bg-gray-400 text-white px-1 py-0.5 rounded font-semibold text-xs {{if this.isCourseRoute 'ml-1'}}" ...attributes data-test-user-label>
    {{this.text}}
    <EmberTooltip @text={{this.tooltipText}} />
  </span>
{{/if}}