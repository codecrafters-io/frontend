{{#if this.solution}}
  {{#if @shouldShowStage1Help}}
    <p class="prose dark:prose-invert mb-3">
      Run the following command to view the files in your repository:
    </p>

    <CopyableTerminalCommandWithVariants @variants={{this.commandVariants}} @onVariantSelect={{this.handleVariantSelect}} class="mb-3" />
  {{/if}}

  <div class="prose dark:prose-invert">
    <p>
      Navigate to the
      <code>{{this.filename}}</code>
      file in your repository.
    </p>
    <p>
      We recommend using an editor / IDE since you'll need to edit this file in the next step.
    </p>
  </div>
{{else if (not @repository.language)}}
  <div class="prose dark:prose-invert">
    <p>
      <span class="text-lg">⚠️</span>
      Complete repository setup to see the instructions for this step.
    </p>
  </div>
{{else}}
  <div class="prose dark:prose-invert">
    <p>
      Navigate to the
      <code>README.md</code>
      file in your repository.
    </p>
    <p>
      You'll need to make changes to this file, so we recommend opening the repository in your editor / IDE of choice.
    </p>
  </div>
{{/if}}