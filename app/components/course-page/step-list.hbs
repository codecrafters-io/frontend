<div class="flex flex-col" ...attributes>
  {{#each @stepList.visibleSteps as |step|}}
    <CoursePage::StepListItem @step={{step}} @isCurrent={{eq this.currentStep step}} @isActive={{eq this.activeStep step}} />
  {{/each}}
</div>