<div class="text-gray-700 prose mb-5" data-test-select-expected-activity-frequency-section>
  <p>
    <b>How often do you intend to practice?</b>
    Learners that maintain a consistent cadence are most likely to meet their learning objectives.
  </p>
</div>

<div>
  <div class="flex items-center flex-wrap max-w-3xl gap-3">
    {{! @glint-expect-error not ts-ified yet }}
    {{#each-in @repository.constructor.expectedActivityFrequencyMappings as |key value|}}
      <TertiaryButton
        class="{{if (eq @repository.expectedActivityFrequency key) 'border-teal-500 text-teal-500'}}"
        {{! @glint-expect-error not ts-ified yet }}
        {{on "click" (fn this.handleSelect key)}}
      >
        {{value}}
      </TertiaryButton>
    {{/each-in}}
  </div>
</div>