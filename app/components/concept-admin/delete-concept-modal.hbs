<ModalBody @allowManualClose={{true}} @onClose={{@onClose}} class="w-full" data-test-delete-concept-modal>
  <div class="mb-6 font-semibold text-2xl text-gray-600 mr-8">
    Delete Concept
  </div>

  <div class="prose mb-4">
    Are you sure you want to delete the
    <span class="font-bold">{{@concept.title}}</span>
    concept?
  </div>

  <div class="prose mb-4">
    All content will be deleted, and will NOT be recoverable.
  </div>

  <div class="prose mb-4">
    You created this concept
    <span class="font-bold">{{date-from-now @concept.createdAt}}</span>, and it currently contains
    <span class="font-bold">{{@concept.blocks.length}}</span>
    blocks.
  </div>

  <div class="prose mb-4 text-red-600">
    To proceed, press and hold the button below for
    <span class="font-bold">3 seconds</span>:
  </div>

  <DangerButtonWithTimedConfirmation @onConfirm={{this.deleteConcept}} @size="regular" data-test-confirm-delete-concept-button>
    Delete
  </DangerButtonWithTimedConfirmation>
</ModalBody>