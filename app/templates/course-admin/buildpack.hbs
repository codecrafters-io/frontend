<div class="bg-white min-h-screen">
  <div class="container mx-auto pt-4 pb-10 px-6">
    <div class="py-3 border-b mb-6">
      <TertiaryLinkButton @route="course-admin.buildpacks" @models={{array @model.buildpack.course.slug}} @size="small" class="pl-1.5 mb-3">
        <div class="flex items-center">
          {{svg-jar "arrow-circle-left" class="w-4 h-4 mr-1.5 text-gray-500"}}

          <span>
            Back
          </span>
        </div>
      </TertiaryLinkButton>

      <div class="flex items-start justify-between">
        <div>
          <h3 class="text-lg font-mono font-bold text-gray-800 mb-2">
            {{@model.buildpack.slug}}
          </h3>
        </div>
        {{!-- <div class="flex flex-col items-end">
          <a
            href={{this.viewDiffLink}}
            class="text-gray-400 flex items-center hover:text-gray-600"
            target="_blank"
            rel="noopener noreferrer"
            data-test-view-diff-link
          >
            {{svg-jar "github" class="inline-flex w-4 mr-1 fill-current"}}
            <span class="text-xs underline font-medium">View diff</span>
          </a>
        </div> --}}
      </div>
    </div>

    <FileContentsCard
      class="mb-4"
      @filename="{{@model.buildpack.slug}}.Dockerfile"
      @code={{@model.buildpack.dockerfileContents}}
      @language="dockerfile"
      data-test-dockerfile-contents-card
    />
  </div>
</div>