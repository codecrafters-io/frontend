{{page-title "Contest"}}

<div class="bg-black dark">
  <div class="container mx-auto lg:max-w-screen-lg pt-6 md:pt-10 pb-10 md:pb-48 px-3 md:px-6">
    <div class="border-b border-gray-800 pb-2 mb-6">
      <div class="flex items-center gap-3 justify-between">
        <h1 class="text-3xl text-gray-200 font-bold tracking-tighter">{{@model.contest.name}}</h1>

        <span class="text-sm text-gray-300 font-semibold">
          Jan 1 - Jan 8
        </span>
      </div>
    </div>
  </div>
</div>