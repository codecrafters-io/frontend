{{page-title "Contest"}}

<div class="bg-black dark">
  <div class="container mx-auto lg:max-w-screen-lg pt-6 md:pt-10 pb-10 md:pb-48 px-3 md:px-6">
    <ContestPage::Header @contest={{@model.contest}} class="mb-6" />
    <div class="flex gap-3 md:gap-6 items-start">
      <div class="flex-grow flex-shrink min-w-0 flex flex-col items-stretch gap-6">
        <ContestPage::LeaderboardCard @contest={{@model.contest}} />
        <ContestPage::HowItWorksCard @contest={{@model.contest}} />
      </div>

      <ContestPage::PrizeDetailsCard @contest={{@model.contest}} class="w-64 xl:w-80" />
    </div>
  </div>
</div>