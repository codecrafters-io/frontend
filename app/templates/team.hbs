{{page-title "Manage Team"}}

<div class="container mx-auto lg:max-w-(--breakpoint-lg) pt-10 pb-48 px-6">
  <div class="flex justify-between flex-wrap">
    <div class="font-semibold text-2xl text-gray-700 mb-6 shrink-0">Manage Team</div>
    {{! @glint-expect-error: component not TS-ified yet }}
    <TeamPage::TeamSelectionDropdown @team={{this.model.team}} @allTeams={{this.model.allTeams}} class="shrink-0 mb-6" />
  </div>

  <div class="flex gap-4 items-start flex-col md:flex-row">
    <div class="w-full md:w-1/3">
      {{#if this.model.team.hasActiveSubscription}}
        {{! @glint-expect-error: component not TS-ified yet }}
        <TeamPage::SubscriptionSettingsContainer @team={{this.model.team}} />
      {{else if this.model.team.canStartSelfServeBillingFlow}}
        {{! @glint-expect-error: component not TS-ified yet }}
        <TeamPage::SetupSubscriptionContainer @team={{this.model.team}} />
      {{else}}
        {{! @glint-expect-error: component not TS-ified yet }}
        <TeamPage::ContactForTeamBillingContainer @team={{this.model.team}} />
      {{/if}}

      {{! @glint-expect-error: component not TS-ified yet }}
      <TeamPage::SlackIntegrationSettingsContainer @team={{this.model.team}} class="mt-4" />
    </div>

    {{! @glint-expect-error: component not TS-ified yet }}
    <TeamPage::MembersContainer @team={{this.model.team}} class="w-full md:w-2/3" />
  </div>
</div>